{
  "args": [
    "callers",
    "encode"
  ],
  "cwd": "<REPO>",
  "env": {},
  "inputs": {},
  "status": 0,
  "stdout": "encode · function · native/codec.c:L2-L2\n  calls ← encode_twice (native/codec.c:L3-L3)\n      3: int encode_twice(int value) { return encode(encode(value)); }\n\nencode · function · native/codec.h:L1-L1\n  no indexed callers — the graph has no incoming call/reference edges for this symbol as written. 2 definitions share the name \"encode\"; a cross-file caller of an ambiguous name is dropped rather than guessed, so this may undercount. Check the name (try the bare symbol, or \"Type.method\"), or find its uses with graft grep \"encode\". Fall back to raw grep -rn only for unindexed files\n",
  "stderr": "",
  "files": {},
  "checkFiles": false
}
