{
  "args": [
    "ask",
    "store",
    "--no-refresh"
  ],
  "cwd": "<REPO>",
  "env": {},
  "inputs": {},
  "mutations": {
    "writes": {
      "repo/graft/.graph/wiring.json": "{ not json"
    },
    "deletes": []
  },
  "status": 0,
  "stdout": "no matching nodes — try different words, or `graft build` if graft/ is empty\n\n[graft] no hits — don't re-ask with new wording; switch tool: `graft grep -i \"<literal>\"` for every occurrence · `graft skeleton <file>` for a file's full API · `graft callers <symbol>` for who-uses.\n",
  "stderr": "",
  "files": {},
  "checkFiles": false
}
