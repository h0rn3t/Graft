{
  "args": [
    "skeleton",
    "src/graph/types.ts"
  ],
  "cwd": "<REPO>",
  "env": {
    "COLUMNS": "80",
    "DO_NOT_TRACK": "1",
    "GRAFT_NO_REFRESH": "1"
  },
  "inputs": {},
  "status": 0,
  "stdout": "graft skeleton — src/graph/types.ts\n- L15-L32 type Kind = | \"file\" | \"class\" | \"function\" | \"method\" | \"interface\" // TS + Go | \"type\" // TS + Go (type alias / named type) | \"enum\" // TS + PHP + Java | \"struct\" // Go only | \"trait\" // PHP only // The generic (tags.scm) breadth tier also emits these — every tree-sitter // grammar's tags.scm uses the tree-sitter tags @definition.<X> vocabulary, and // module/constant/variable are common across the long tail (Ruby modules, // Rust consts, top-level lets, …). Kept distinct rather than coerced so the // breadth tier's kinds read truthfully in cards/skeleton. | \"module\" | \"constant\" | \"variable\";\n- L40-L40 type Confidence = \"lsp_resolved\" | \"lsp_dispatch\" | \"extracted\" | \"inferred\";\n- L43-L43 type SummaryState = \"pending\" | \"ready\" | \"stale\";\n- L47-L50 interface Crux\n- L52-L93 interface NodeV1\n- L95-L101 type Relation = | \"contains\" // file → symbol, class → method (structural) | \"calls\" // function → function it invokes | \"imports\" // file → module | \"references\" // symbol → symbol it names but doesn't call | \"implements\" // TS: class → interface | \"extends\";\n- L103-L108 interface EdgeV1\n- L114-L118 interface ScopeV1\n- L120-L132 interface GraphV1\n",
  "stderr": "",
  "files": {}
}
