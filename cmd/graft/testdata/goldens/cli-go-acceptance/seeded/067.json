{
  "args": [
    "ask",
    "compute set",
    "--source"
  ],
  "cwd": "<REPO>",
  "env": {
    "COLUMNS": "80",
    "DO_NOT_TRACK": "1",
    "GRAFT_NO_REFRESH": "1"
  },
  "inputs": {},
  "status": 0,
  "stdout": "graft ask — \"compute set\"  (lexical)\n\n1. computeStamp · function  [symbol]\n   src/graph/extract-cache.ts:L144-L160\n   function computeStamp(): string | null\n\n```\nL144: function computeStamp(): string | null {\nL145:   try {\nL146:     const self = fileURLToPath(import.meta.url);\nL147:     // `.js` when running from `dist/`, `.ts` under tsx — take the extension from\nL148:     // our own filename rather than guessing which layout we're in.\nL149:     const dir = dirname(self);\nL150:     const hashed = stampDir(dir, extname(self), packageVersion(dir) ?? \"\");\nL151:     if (hashed) return hashed;\n… (excerpt; full definition at src/graph/extract-cache.ts:L144-L160; rerun with --full)\n```\n\n2. computeIdf · function  [symbol]\n   src/ask/ask.ts:L230-L235\n   function computeIdf(docBags: Array<Set<string>>): Map<string, number>\n\n```\nfunction computeIdf(docBags: Array<Set<string>>): Map<string, number> {\n  const df = new Map<string, number>();\n  for (const bag of docBags)\n    for (const t of bag) df.set(t, (df.get(t) ?? 0) + 1);\n  return idfFromDf(df, docBags.length);\n}\n```\n\n3. computeInDegree · function  [symbol]\n   src/graph/map.ts:L110-L117\n   function computeInDegree(graph: GraphV1): Map<string, number>\n\n```\nfunction computeInDegree(graph: GraphV1): Map<string, number> {\n  const deg = new Map<string, number>();\n  for (const e of graph.edges) {\n    if (!WALK_RELATIONS.has(e.relation)) continue;\n    deg.set(e.target, (deg.get(e.target) ?? 0) + 1);\n  }\n  return deg;\n}\n```\n\n4. computeInDegree · function  [symbol]\n   src/search/grep.ts:L126-L133\n   function computeInDegree(graph: GraphV1): Map<string, number>\n\n```\nfunction computeInDegree(graph: GraphV1): Map<string, number> {\n  const deg = new Map<string, number>();\n  for (const e of graph.edges) {\n    if (!WALK_RELATIONS.has(e.relation)) continue;\n    deg.set(e.target, (deg.get(e.target) ?? 0) + 1);\n  }\n  return deg;\n}\n```\n\n5. computeMapDirs · function  [symbol]\n   internal/graph/map.go:L159-L226\n   func computeMapDirs(nodes []NodeV1, inDegree map[string]int, maxDirs, hubsPerDir int, stripPrefix string) mapDirComputation\n\n```\nL159: func computeMapDirs(nodes []NodeV1, inDegree map[string]int, maxDirs, hubsPerDir int, stripPrefix string) mapDirComputation {\nL160: \tfileNodes := make([]NodeV1, 0)\nL161: \tfor _, node := range nodes {\nL162: \t\tif node.Kind == Kind(\"file\") {\nL163: \t\t\tfileNodes = append(fileNodes, node)\nL164: \t\t}\nL165: \t}\nL166: \ttotalFiles := len(fileNodes)\n… (excerpt; full definition at internal/graph/map.go:L159-L226; rerun with --full)\n```\n\n6. ChildRun · interface  [symbol]\n   src/graph/workspace.ts:L198-L212\n   interface ChildRun\n\n```\nL198: interface ChildRun {\n…\nL205:    * A body-only incidental collision has `coverageStrong === 0`. */\nL206:   coverageStrong: number;\nL207:   /** Exact baseline top-hit coverage used only to compute the workspace top lock. */\nL208:   baselineCoverage: number;\nL209:   baselineCoverageStrong: number;\nL210:   /** Present only for the internal file-aware path. */\nL211:   ranking?: AskRankingMetadata;\n… (excerpt; full definition at src/graph/workspace.ts:L198-L212; rerun with --full)\n```\n\n7. EnrichOptions · interface  [symbol]\n   src/graph/enrich.ts:L32-L47\n   interface EnrichOptions\n\n```\nL32: export interface EnrichOptions {\nL33:   /** When present, (re)compute meaning for stale/pending nodes. Absent → cache only. */\nL34:   summarizer?: CruxSummarizer;\nL35:   /** Max files summarized in parallel (each is one LLM call). Default {@link DEFAULT_CONCURRENCY}. */\nL36:   concurrency?: number;\nL37:   /** Progress is reported per file (one LLM call each), as files finish — not per node. */\nL38:   onProgress?: (info: { index: number; total: number; node: string }) => void;\nL39:   /**\n… (excerpt; full definition at src/graph/enrich.ts:L32-L47; rerun with --full)\n```\n\n8. extractorStamp · function  [symbol]\n   src/graph/extract-cache.ts:L139-L142\n   function extractorStamp(): string | null\n\n```\nexport function extractorStamp(): string | null {\n  if (memoizedStamp === undefined) memoizedStamp = computeStamp();\n  return memoizedStamp;\n}\n```\n",
  "stderr": "",
  "files": {}
}
