{
  "args": [
    "init",
    "<REPO>",
    "--agents",
    "kiro",
    "--no-build"
  ],
  "env": {
    "DO_NOT_TRACK": "1",
    "GRAFT_MCP_NPX": "1",
    "HOME": "<HOME>",
    "USERPROFILE": "<HOME>"
  },
  "gitDirs": [
    "repo"
  ],
  "dirs": [
    "home",
    "home/.adal",
    "home/.codeium/windsurf",
    "home/.codex",
    "home/.config/opencode",
    "home/.cursor",
    "home/.gemini/config",
    "home/.grok",
    "home/.hermes",
    "home/.kiro",
    "repo"
  ],
  "inputs": {},
  "mutations": {
    "writes": {
      "repo/.kiro/settings/mcp.json": "{ not json"
    },
    "deletes": []
  },
  "status": 0,
  "stdout": "",
  "stderr": "- removed <REPO>/AGENTS.md (fenced graft section) — agent not selected\n- removed <REPO>/.adal/skills/graft/SKILL.md (graft-owned instruction file) — agent not selected\n- removed <REPO>/.cursor/rules/graft.mdc (graft-owned instruction file) — agent not selected\n- removed <REPO>/GEMINI.md (fenced graft section) — agent not selected\n- removed <REPO>/.grok/skills/graft/SKILL.md (graft-owned instruction file) — agent not selected\n- removed <REPO>/.github/copilot-instructions.md (fenced graft section) — agent not selected\n- removed <REPO>/.windsurf/rules/graft.md (graft-owned instruction file) — agent not selected\n- removed <HOME>/.codex/config.toml ([mcp_servers.graft]) — agent not selected\n- removed <REPO>/opencode.json (mcp.graft) — agent not selected\n- removed <REPO>/.cursor/mcp.json (mcpServers.graft) — agent not selected\n- removed <REPO>/.gemini/settings.json (mcpServers.graft) — agent not selected\n- removed <REPO>/.grok/config.toml ([mcp_servers.graft]) — agent not selected\n- removed <REPO>/.claude/settings.json (statusline + hooks + allowlist + footer regex) — agent not selected\n- removed <REPO>/.claude/helpers/graft-statusline.cjs (statusline shim) — agent not selected\n- removed <REPO>/.claude/helpers/graft-hooks.cjs (hooks shim) — agent not selected\n- removed <REPO>/.claude/skills/graft/SKILL.md (graft skill) — agent not selected\n- removed <REPO>/.mcp.json (mcpServers.graft) — agent not selected\n- removed <HOME>/.claude/helpers/graft-hooks.cjs (hooks shim (user level)) — agent not selected\n- removed <HOME>/.claude/settings.json (SessionStart / UserPromptSubmit / PostToolUse / Stop / SubagentStop) — agent not selected\n- removed <HOME>/.claude.json (mcpServers.graft) — agent not selected\n- removed <HOME>/.codex/hooks/graft/graft-hooks.cjs (post-edit hook shim) — agent not selected\n- removed <HOME>/.codex/hooks.json (SessionStart / UserPromptSubmit / PostToolUse / Stop) — agent not selected\n✓ kiro: <REPO>/.kiro/steering/graft.md (unchanged)\n✓ mcp kiro: <REPO>/.kiro/settings/mcp.json (skipped-unparseable)\n· skipped graph build\n\n         ,_---~~~~~----._\n  _,,_,*^____      _____``*g*\\\"*,\n / __/ /'     ^.  /      \\ ^@q   f\n[  @f | @))    |  | @))   l  0 _/                      __ _\n \\`/   \\~____ / __ \\_____/    \\        __ _ _ __ __ _ / _| |_\n  |           _l__l_           I      / _` | '__/ _` | |_| __|\n  }          [______]           I    | (_| | | | (_| |  _| |_\n  ]            | | |            |     \\__, |_|  \\__,_|_|  \\__|\n  ]             ~ ~             |     |___/             v0.0.0\n  |                            |\n   |                           |\n\n  1. build the graph     graft build\n  2. restart your agent  a new session picks up graft automatically\n  3. code as usual       ask your agent to fix a bug or explain a flow —\n                         it now answers from the graph\n  4. explore by hand     graft ask \"where is auth handled?\" · graft callers <fn>\n\n  share it: git add .claude && git commit — teammates run `graft build` for their own local graph\n",
  "files": {
    "home/.claude.json [644]": "{\n  \"numStartups\": 3,\n  \"mcpServers\": {\n    \"mine\": {\n      \"command\": \"mine\"\n    }\n  }\n}\n",
    "home/.codex/config.toml [644]": "[mcp_servers.other]\ncommand = \"other\"\n",
    "home/.codex/hooks.json [644]": "{\n  \"hooks\": {\n    \"Stop\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"echo mine\"\n          }\n        ]\n      }\n    ]\n  },\n  \"custom\": 1\n}\n",
    "home/.graft/update-check.json [644]": "{\n  \"latest\": \"0.0.1\",\n  \"checkedAt\": 4102444800000\n}",
    "repo/.claude/settings.json [644]": "{\n  \"2\": \"numeric key\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm test:*)\"\n    ],\n    \"deny\": [\n      \"Bash(rm:*)\"\n    ]\n  },\n  \"hooks\": {\n    \"Stop\": [\n      {\n        \"hooks\": [\n          {\n            \"type\": \"command\",\n            \"command\": \"echo done\"\n          }\n        ]\n      }\n    ]\n  },\n  \"statusLine\": {\n    \"type\": \"command\",\n    \"command\": \"my-statusline\"\n  }\n}\n",
    "repo/.cursor/hooks.json [644]": "{\n  \"version\": 1,\n  \"hooks\": {\n    \"postToolUse\": [\n      {\n        \"matcher\": \"Read|Grep|Glob|Search|Shell\",\n        \"command\": \"node \\\".cursor/hooks/graft-hooks.cjs\\\" cursor-post-tool\"\n      }\n    ],\n    \"afterMCPExecution\": [\n      {\n        \"command\": \"node \\\".cursor/hooks/graft-hooks.cjs\\\" cursor-mcp\"\n      }\n    ],\n    \"sessionEnd\": [\n      {\n        \"command\": \"node \\\".cursor/hooks/graft-hooks.cjs\\\" cursor-session-end\"\n      }\n    ]\n  }\n}\n",
    "repo/.cursor/hooks/graft-hooks.cjs [755]": "#!/usr/bin/env node\nconst fs = require('fs');\nconst { spawnSync } = require('child_process');\nconst BAKED = \"<BAKED>\";\n\n// The graft binary that wrote this shim, else the first graft on PATH. A\n// missing binary exits 0 so the host never sees a failing hook.\nconst binary = BAKED && fs.existsSync(BAKED) ? BAKED : 'graft';\nconst result = spawnSync(binary, ['_hook', process.argv[2]], { stdio: 'inherit' });\nif (result.error) process.exit(0);\nprocess.exit(result.status ?? 1);\n",
    "repo/.cursor/mcp.json [644]": "{\n  \"mcpServers\": {\n    \"other\": {\n      \"command\": \"x\",\n      \"args\": [\n        \"1e3\",\n        1000,\n        0.1\n      ]\n    }\n  }\n}\n",
    "repo/.github/workflows/ci.yml [644]": "on: push\n",
    "repo/.kiro/settings/mcp.json [644]": "{ not json",
    "repo/.kiro/steering/graft.md [644]": "---\ninclusion: always\n---\n## Graft — repo context graph\n\nThis repo is indexed in `graft/`: a prebuilt graph of every symbol, its exact\nfile:line span, and who calls what. Get context from graft before grepping or\nopening source files. Every query refreshes the graph first, so results include\nuncommitted edits; there is no need to run `graft build` after editing.\n\n- `graft ask \"<question>\" --source`: how X works or where Y lives, as ranked\n  hits with code excerpts inlined. Skip it when you can already name the symbol.\n- `graft grep \"<name or literal>\"`: every occurrence over indexed files, grouped\n  by enclosing symbol. Use it for exhaustive work and to jump to a symbol you\n  can name; raw `grep -rn` is only for files graft doesn't index.\n- `graft skeleton <file>`: a file's signatures and spans, ~10× cheaper than\n  reading it.\n- `graft callers <symbol>`: exact call edges. `--direction out` for\n  dependencies, `--depth 2` before a rename or signature change, `--depth all`\n  before a multi-file refactor.\n- `graft map`: orientation when you're new to the repo.\n\nAct on the answer: cite and edit from the returned spans without re-opening\nfiles to confirm them. Weak hits mean switch tool, not re-ask the same question\nreworded; a `[graft] weak match` notice names the tool and term to try. When an\nexcerpt ends in `… +N lines`, rerun with `--full` or open exactly that range,\nnever the whole file. In a multi-repo workspace, hits carry `[scope/]`\nlabels; narrow with `--in <scope>/`. With the graft MCP server connected, the\nsame tools are `graft_find_code`, `graft_find_all`, `graft_file_api`,\n`graft_trace_calls` and `graft_repo_map`.\n",
    "repo/AGENTS.md [644]": "# Team notes\n\nKeep PRs small.\n",
    "repo/graft/.cache/wiring-stamp.json [644]": "{\n  \"version\": \"0.0.0\",\n  \"hosts\": [\n    \"kiro\"\n  ],\n  \"opts\": {\n    \"global\": true,\n    \"mcp\": true,\n    \"hooks\": true,\n    \"statusline\": true\n  },\n  \"at\": \"<AT>\"\n}",
    "repo/src/main.ts [644]": "export function main() { return 1; }\n"
  },
  "checkFiles": true
}
